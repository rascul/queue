image: rust:latest

test:cargo:
  script:
  - rustc --version && cargo --version
  - cargo test --all --verbose

pages:
  stage: deploy
  script:
  - cargo doc
  - mv target/doc public
  - mv docs/index.html public/
  artifacts:
    paths:
    - public
  only:
  - master
